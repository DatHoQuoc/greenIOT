package com.greeniot.greensense.entity.enums;

/**
 * Presentation hint consumed by the frontend timeline ({@code <i className="green"/>}).
 * Kept server-side so the client does not re-derive styling from copy.
 */
public enum EventTone {
    GREEN, TEAL, GRAY, AMBER, RED
}
