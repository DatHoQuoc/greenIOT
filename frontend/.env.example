# Copy to .env.local for local development.
#
# NEXT_PUBLIC_* is inlined into the bundle at BUILD time, not read at runtime — changing
# this in a running container does nothing, the image has to be rebuilt.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
